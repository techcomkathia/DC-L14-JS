fetch('url')
    .then(respostaAPI => respostaAPI.json())
    .then(//capatura o JSON retornado pelo then anterior e manipula
    )
    .then(//quantos thens forem nessecários, quantos then forem nessecários
    )
    .catch(erro => //capta o erro e trata esse erro
    )

//acesse a API viacep e faça uma requisição get para o seu cep. Mostre os dados no console: logradouro, localidade, uf

